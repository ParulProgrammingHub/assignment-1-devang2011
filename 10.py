pri = float(input(" Enter the principal "))
rate = float(input(" Enter the interest rate "))
years= float(input(" Enter the number of years "))
def simple_interest(pri,rate,years):
       simple_interest=float((pri*rate*years/100))
       returnsimple_interest
print(" Simple interest is ", simple_interest(pri,rate,years))
